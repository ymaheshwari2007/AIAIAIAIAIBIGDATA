[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff.lint]
# Lean ruleset: ruff's real-bug core (E4/E7/E9 syntax+logic, F pyflakes) + import sorting (I).
# Deliberately NOT the opinionated style rules (UP/DTZ/BLE/PIE/PLW) — keep lint signal, drop noise.
select = ["E4", "E7", "E9", "F", "I"]
